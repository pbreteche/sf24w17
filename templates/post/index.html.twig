{% extends 'base.html.twig' %}

{% block title %}{{ 'post.last_title'|trans }}{% endblock %}

{% block body %}
    <h1>{{ 'post.last_title'|trans }}</h1>
    <p>{{ 'àéïôù' | transliterate }}</p>
    <form action="{{ path('app_post_search') }}">
        <label for="search-query">{{ 'action.search'|trans }}</label>
        <input type="search" id="search-query" name="q">
        <button class="btn btn-secondary">🔎</button>
    </form>
    <p>{{ 'post.count'|trans({posts: posts|length}) }}</p>
    <a href="{{ path('app_admin_post_new') }}">{{ 'action.new'|trans }}</a>
    {{ include('post/_list.html.twig', {empty_message: 'Aucune publication actuellement'}) }}
{% endblock %}
